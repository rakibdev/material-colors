{
  "name": "@rakibdev/material-colors",
  "version": "1.0.0",
  "description": "Material You color generator.",
  "author": "https://github.com/rakibdev",
  "repository": "https://github.com/rakibdev/material-colors.git",
  "publishConfig": { "registry": "https://npm.pkg.github.com" },
  "files": [
    "build"
  ],
  "type": "module",
  "exports": {
    ".": {
      "import": "./build/main.js",
      "types": "./build/main.d.ts"
    }
  },
  "scripts": {
    "build": "bun build:src && bun build:types",
    "build:src": "bun build ./typescript/main.ts --outdir ./build --minify",
    "build:types": "tsc --project ./tsconfig.types.json",
    "release": "bun run build && bun publish"
  },
  "devDependencies": {
    "@types/bun": "^1.2.15",
    "typescript": "^5.8.3"
  }
}
